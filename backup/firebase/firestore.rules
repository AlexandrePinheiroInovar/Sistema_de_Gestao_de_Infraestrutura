rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // ============= REGRAS DE DESENVOLVIMENTO - MUITO PERMISSIVAS =============
    // APENAS PARA TESTE - REMOVER EM PRODUÇÃO
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}